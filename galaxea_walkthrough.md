#################################知识一（奖励数据的录制技巧）
有，而且你记得的方向基本对，不过官方更常用的说法不是“假成功/假失败数据”，而是：

false positives：明明没成功，分类器却判成成功
false negatives：明明成功了，分类器却没给成功

官方 HIL-SERL 网站先概括说，他们会先遥操作收集正样本和负样本，用来训练一个二分类 reward classifier。

更具体的技巧写在官方 franka_walkthrough.md 里，讲得很明确：

录 reward classifier 数据时，默认所有 transition 都记成负样本，只有你按键标记的那些才记成正样本。
官方明确建议：为了让分类器对 false positives 更稳，负样本最好比正样本多 2 到 3 倍。
而且负样本不是随便拍失败就行，应该专门覆盖容易误判的失败模式，例如：
插在错误位置
只插进去一半
拿着物体但还没真正插入
USB 任务里还特别提到：插进了错误的 USB 口 这种“很像成功但其实是失败”的情况。

这其实就是你说的“假成功/假失败”那个意思，只是官方没有用这个中文词。
更准确地说，官方是在强调：

要专门收集那些“看起来像成功、但实际上是失败”的负样本。

比如文档里就举例说：

如果分类器会把“RAM 条举在空中”误判成成功，那就要多收这种情况的负样本。

另外，官方还说在录 demo 时，如果你发现 reward classifier 出现 false positive 或 false negative，优先做法不是只调阈值，而是继续补针对性的 classifier 数据；必要时甚至增加 classifier 相机或图像。

所以一句话回答你：

有，官方确实强调了这种思路：reward classifier 要靠“成功样本 + 大量覆盖失败模式的负样本”来变稳，尤其要收那些“很像成功但其实失败”的样本，也就是你理解的那种“假成功/假失败”边界数据。


#########################################
知识一小结：成功状态要记录，失败状态要更多，而且要重点记录更多类似成功但其实失败的画面（如果已经训练出来的ckpt模型准确率因为这个原因不高，要重新记录更多这种数据）
#########################################



####
####
####


###########################################知识二（训练细节）
任务一旦判成功就结束并复位，那策略学到的到底是什么？部署时怎么连贯？

结论是：

官方这类 HIL-SERL 任务本来就是“按 episode 定义的单次任务”，不是一个无限连续流程。
也就是说，对 USB 插入这种任务，“插成功”本身就是这一回合的终点。demo 录制时，官方明确写的是：一旦 reward classifier 判成功，或者超时，机器人就 reset；录满指定数量的成功 demo 后脚本结束。RAM 插入和 USB pickup/insertion 两个 walkthrough 都是这么写的。

你自己的运行日志也符合这个逻辑：
actor 每个 episode 结束后会打印 [actor-episode-end]，然后立刻进入“正在准备复位 / 开始向底层发送复位坐标 / 单臂平滑复位”的流程。也就是说，你当前代码里，成功或 episode 结束后，后续动作不是“继续做下一个任务步骤”，而是 reset 到下一回合起点。

所以回答你最核心的问题：

1）成功后模型“怎么做”？

官方默认答案是：不继续做别的动作，而是把这一回合视为完成，然后 reset。

因为这类 task 的目标函数就是：

从初始状态出发
完成一次插入
成功即终止

在这种定义下，policy 学的不是“永远不停地工作”，而是：

给定一个新的初始状态，尽快把任务做成功。

这和工业里常见的“单工位循环任务”是一样的。不是策略自己负责决定“下一件事干嘛”，而是外部流程负责把环境恢复到下一个起点，再让策略重新执行一次。

####################################################
知识二小结：判断自己的模型和训练方式，是否成功后就复位，符合官网设计
######################################################


###
###
###

####################知识三（奖励和训练的相机不需要完全一致）
奖励的相机和训练的相机不需要一致（画面和个数）
（1）奖励的相机：只要可以准确识别成功画面即可
（2）覆盖整体状态（场景和夹爪）和局部状态（夹爪遮挡的部分）
##################################



###
###
###


########################知识四（动作缩放）
动作缩放
官方不是靠一个数学公式“算出来正确”，而是靠这类原则去定：

policy 输出的是小的 end-effector delta action
环境层把它缩放成真实机器人每控制周期的平移/旋转小增量
然后看它是否满足：
安全
稳定
能完成任务
不会抖动/冲过头/来不及到位

官方 repo 和说明里，动作都是这种 delta pose control 思路，不是直接输出绝对位姿。官方 issue 里也明确提到，SERL/HIL-SERL 的动作是小增量，因为每个控制周期都只走一点点。

所以“官网怎么知道是对的”，更准确地说是：

他们是通过真实机器人上的控制频率、动作平滑性、是否容易成功、是否会震荡或越界来调出来的。
不是随便填，也不是论文里给一个放之四海而皆准的固定值。


####


#####################知识五（扰动的设置）
增加reset的扰动
目前我增加了：
RANDOM_RESET = True
RANDOM_XY_RANGE = 0.01

而你的 GalaxeaUSBEnv._build_reset_target() 实现是：

先复制 RESET_POSE
如果 RANDOM_RESET=True
就只对 reset_pose[:2] 加一个 [-xy_range, xy_range] 的均匀随机扰动。

所以结论很明确：

你有没有加这种扰动？

有。

你加的是哪种扰动？

只加了 XY 平移扰动。

你有没有像官方一样加 RZ/朝向扰动？

目前没有。

因为我在你这份 GalaxeaUSBEnv._build_reset_target() 里只看到了：

reset_pose[:2] += ...

没有看到对旋转、尤其是 RZ 的随机化。


###
###
###


########################知识六（和官网的大致区别）
你当前 env 对训练效果最可能有影响的“大区别”

如果只挑最影响训练效果的，我会按这个顺序排：

1. reward 完全依赖 classifier，而不是 env 内部几何阈值

这是你和官网最大的训练机制差异之一。

2. state 比官网少很多

没有 vel / force / torque，这对插接任务可能很伤。



4. reset 少了官方的 recover / param switch / joint reset 周期

这会影响长时间稳定性和 reset 一致性。

5. reset 扰动只做了 XY，没有 RZ

鲁棒性分布比官网窄。

十五、给你一个非常直接的结论

你的 env 不是“和官网一样，只是把 Franka 换成了 Galaxea”。
它已经发生了这些级别的变化：

机器人通信后端变了
dual arm 架构变了
reward 放置位置变了
reward少了一种（我没有根据目标姿态来判定奖励，只靠视觉）
state 定义变了
gripper 控制语义变了
reset 机制变了
reset 扰动范围变了
camera pipeline 也变了

所以如果训练效果不好，不能只怪算法本身。
至少这一层 env，就已经和官网存在很多会影响学习行为的差异。


###
###
###
###################知识七（归一化理解）
模型输出一个归一化动作
例如单臂 7 维动作。
先 clip 到 [-1, 1]
防止输出过大。
把前 6 维解释成“这一步的末端位姿增量”
平移增量：action[:3] * POS_SCALE
旋转增量：action[3:6] * ROT_SCALE
把这个增量加到“当前末端位姿”上
也就是：
next_xyz = current_xyz + delta_xyz
next_quat = 当前姿态 + 旋转增量（通过 apply_delta_rotation）
再做安全裁剪
新目标位姿还会过工作空间限制，避免超边界。
最后发送给 ROS / 机械臂
这个“裁剪后的目标末端位姿”才是最终发下去的控制目标。

所以你这句话：

clip再动作缩放后是一次输出的位置增量，再转换成最终的末端位姿吗？

更准确地说是：

先 clip，再缩放得到“本步末端位姿增量”，再加到当前末端位姿上，得到最终目标末端位姿。

再补一个关键点：

平移/旋转 是这样走“增量 -> 目标位姿”的
夹爪 不是这样，它不是连续位姿增量，而是：
-1 -> close
0 -> hold
+1 -> open
然后再映射成硬件量程。

还有一个例外你要记住：

reset 不走这条增量链路。
reset 是直接给绝对目标位姿，不是模型输出动作再 clip/scale。




###
###
###


########################知识八（bug）

#####
      （bug1）
如果Mode 2 / script_control_enabled=True 时继续 env.step()，同时等待切回 Mode 0，会造成等待mode0前末端自己运动。
如果Mode 2 / script_control_enabled=True 时不再继续 env.step()，只等待切回 Mode 0。末端不会自己动。
目前demos脚本和奖励数据脚本都这么修改从而不让末端自己移动了


bug来源可能：
reset后，等待mode0之前，会发布：
actions = np.zeros(...)
env.step(actions)（零增量的意思）
发布令零增量从而保持末端不动，但是我怀疑，零增量仍然会自己动，从而造成末端自己慢慢微小运动。


解决了：末端在reset后，等待mode0之前，末端自己慢慢移动的bug就是所谓的reset后发布0增量所谓维持原位置是错误的，

验证办法：reset后，等待五秒去处发布零增量，五秒后开始发布零增量，且不打开mode0,发现确实一发布零增量就开始移动。

所以不影响actor，因为actor不发布零增量，发布的是网络认为应该输出的增量，所以不会发生这个问题。
所以只在demos脚本和奖励数据脚本去除发布零增量等待mode0,改成完全静默无动作，等待mode0来临即可。


#####
      （bug2）

demos数据可视化后可以发现，把reset过程中的一帧进行录制了，不知道影响大不大？


#####
#####
#####

########################知识九（归一化后的动作缩放）

调试归一化思路：
    网络/脚本输出 1、0.5、-1 这些归一化动作后，底层目标位姿是否真按 0.01 m / 0.05 rad 这个比例变化。”
    不必纠结是不是把末端位姿归一化【-1,+1】，是输出增量，输出增量+1,对应（+1）*action_scale
    末端位姿起基准作用，在反馈的末端位姿下进行增量控制
      更准确地说：

       不是把“末端位姿本身”归一化到 [-1,+1]
       而是把动作增量定义在 [-1,+1]
       然后进入 env 后：
       平移增量 = action[:3] * action_scale
       旋转增量 = action[3:6] * action_scale
       再把这个增量加到当前反馈/当前记录的末端位姿上
       得到下一步目标末端位姿

所以：

当前末端位姿是基准，归一化的是“本步增量动作”，不是“绝对位姿”。

调试步骤：
  （1）确认确实归一化+真的应用了动作缩放
  （2）如果 POS_SCALE = 0.01   +ROT_SCALE = 0.05   # 0.05 rad，那就是归一化后输出1就是移动0.01m/转动0.05rad。


测试结果（+-1归一化+位置0.01+旋转0.05的缩放）：最终判断是：
  归一化到缩放这条链没大问题，特别是 x 和 rx/ry/rz 已经验证得很漂亮；但在你这个 reset 姿态附近，y、z 方向的真实执行有明显耦合和方向不对称，尤其正方向更明显。


  ###
  ###
  ###

  ###############################知识十（存入的buffer和demo_buffer数据分析）

  buffer 输出说明三件事：

reward / done / mask 存储是对的
所以 return=1 的成功 transition 没丢。
success=0 是日志/info 字段没统一，不是 reward 没触发。
夹爪三值存储是对的，但前 6 维动作存在超过 [-1,1] 的问题，需要在 actor 存 buffer 前 clip。

你现在最应该立刻修的是：

success 字段统一 + stored_actions[:6] clip 到 [-1,1]


####
####
####


###############################知识十一（相机输入）
  总结：按照奖励数据，demos，rlpd需要最大的相机打开ENV_IMAGE_KEYS
           即：
              （1）都使用最大个数相机录制数据，
              （2）rlpd和classifer时选择合适的相机作为输入。

具体任务的config：
###表示本任务环境实际打开/采集哪些相机。后面的 demo、reward classifier、RLPD policy 都只能从这里面选。所以它应该覆盖本任务所有脚本里需要用到的最大相机集合。
1,ENV_IMAGE_KEYS = ["head_rgb", "left_wrist_rgb", "right_wrist_rgb"]

###表示 RLPD actor/critic 策略网络实际吃哪几路图像。demo 里可以有三路，但只要至少包含这里需要的两路，RL 训练就可以只用两路。
2,image_keys = ["head_rgb", "right_wrist_rgb"]

###表示奖励分类器实际吃哪几路图像。你 classifier 是三路训练出来的，所以 RLPD 在线训练时也要给它三路 obs。
3,classifier_keys = ["head_rgb", "left_wrist_rgb", "right_wrist_rgb"]


###
###
###


#######################知识十二（训练速度提升）

（1）ENV_IMAGE_KEYS多+image_keys 少，可以提升learner+actor侧训练速度。
（2）ENV_IMAGE_KEYS少+image_keys 少，可以进一步提升actor训练速度（减轻了图像处理压力），learner侧几乎不影响。
   但是classifer必须重新训练成ENV_IMAGE_KEYS对应的相机个数
  
（3）有时候actor输出动作特别慢：考虑ssh连接问题  +  服务器显卡和别人公用，功率极大下降


###
###
###

#####################知识十三（检查数据）


（1）检查demos图像和数据
（2）检查buffer的图像和数据，归一化，夹爪，奖励
（3）图像不要公用，各取所需，采集数据的时候也是
