version:
(1)record_success_fail.py+train_reward_classifier.py（cpu）
指令：
  python record_success_fail.py
  JAX_PLATFORMS=cpu python train_reward_classifier.py --exp_name=galaxea_usb_insertion

环境：
环境依赖清单：保存在了example下
环境备份：hilserl_gpu_cf_backup

cpu可用，gpu不可用训练奖励函数的环境冲突：
yTorch 是被真实环境env链拉进来的，主要影响真机脚本；
TensorFlow 是被 train_utils 拉进来的，影响 train_reward_classifier.py 和 train_rlpd.py。




api key(hilserlKey):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



2,目前支持单阶段奖励分类器（##无多阶段的奖励分类##）




3,classifier_data目录下的pkl文件一起进入训练，所以保证pkl文件记录的状态：
             ###########
              是对应同一阶段的状态
             ###########

4,具体任务的config（和wrapper统计目录的config）：
  （1）#############config的总默认设置
    DefaultTrainingConfig是写死的参数，必须在这个config中修改


  （2）#############config的其他默认配置
    def get_environment(
        self,
        fake_env: bool = False,
        save_video: bool = False,   
        classifier: bool = False,   
        use_vr: bool = True,
    ):


  （3）########是可调用的配置，如果其他脚本调用这个配置：
    例如env = env_config.get_environment(
        fake_env=False,
        save_video=False,
        classifier=False,
    )
                
                ###################
    则可以在其他脚本修改，如果没有调用，只能在config中修改
        建议在其他脚本调用，这样config保持默认配置，其他脚本调用后按需要修改自己的配置
                ######################


5,总config（和mappings统计目录的config）：
         #########################
       总算法和接口等默认配置，不涉及具体任务 
         #########################


6,#env环境把任务参数全部外置到 config总路->具体任务下的config（在这里修改具体任务的所有配置），底层 env 只负责双臂控制、观测、显示、录像和安全裁剪；


7,相机报错：
先检查接口（接口不对报错）
在还相机接口位置（接口插笔记本的位置不对会爆性能，插对位置才可以让三个相机共存）


8,actor/learner 运行指令：

python train_rlpd.py \
  --exp_name=galaxea_usb_insertion_single \
  --actor=True \
  --ip=localhost \
  --demo_path=./demo_data_single \
  --checkpoint_path=./rlpd_checkpoints_single \
  --debug=True


9,更换任务后，按如下更改后可成功转换任务（涉及保存路径的修改都在对应生成代码的最后面的部分里有定义，在那里修改）：
  （1）exp_name对应：
     先选中设置的config_mapping里的配置类，
     从而装配
       env（单/双臂配置，更换本次任务需要的单/双臂的设置）；
       wrapper（import的具体目标任务wrapper的正确路径）；
       classifier（具体任务的config的代码下面有路径选择，填写本次任务需要导入的奖励分类器的ckpt，
                         由于奖励分类器无纬度，只有相机，所以不容易发现，路径必须写对）；
       rlpd（import的具体目标任务config的正确路径）；


  （2）actor/learner=True对应：启动actor/learner

  （3）ip：用来actor端连接learner端

  （4）demo_path对应：
    本次任务对应的专家演示demos保存的路径
    actor/learner初始化demo里的数据确定双臂还是单臂等，后续的输入纬度和demo纬度对齐才会进行后续
    所以demo录制脚本的demos保存文件夹需要按需要重新设置

  （5）checkpoint_path 对应目录：rlpd_checkpoints_X
           仅由启动指令中的 --checkpoint_path 决定路径

rlpd_checkpoints_X/
├── checkpoint_* 
│   （由 learner 保存的 RL policy checkpoint；
│    可用于断点续训；actor 的评估模式可读取该权重输出动作）
│
├── buffer/
│   （由 actor 写入、learner 读入；
│    存的是 actor 在环境中实际执行后得到的在线 transition，
│    包括策略动作、随机动作，以及被人类接管后替换成 intervene_action 的动作）
│
└── demo_buffer/
    （由 actor 写入、learner 读入；
     主要存训练过程中的在线人类干预片段/intervention transitions；
     不等同于最初通过 --demo_path 加载的离线 demos）

     另外，提前录制的离线 demos 不在 checkpoint_path 里定义，
     而是由 --demo_path 单独指定；learner 启动时会先加载这些离线 demos，
    再结合 checkpoint_path 下的 buffer/ 和 demo_buffer/ 一起训练。


10,数据来源：
learner 启动时
demo_buffer：先加载你录好的 demos
replay_buffer：一开始通常是空的，除非你从旧的 checkpoint_path/buffer/ 恢复到历史在线数据
actor 开始跑之后

actor 才会开始生成新的在线数据，写进：

buffer/
learner 侧对应的 replay_buffer


11, 把多阶段 classifier 列入 roadmap。 官方 USB walkthrough 明确支持多阶段，你现在可以先把接口铺好
classifier_stages = [
    {"name": "pickup", "image_keys": [...]},
    {"name": "align", "image_keys": [...]},
]


12,补 preflight check 和 3 个 smoke tests。 这会直接减少你最近反复出现的单/双臂维度和路径错误。

13,本地进入examples目录，把demos和classifer文件夹传送到服务器：
rsync -avz -e "ssh -p 2122" demo_data_single \
lixiang@service.qich.top:/home/hddData/User/lixiang/hilserl_workspace/HIL-SERL-Project/examples/

rsync -avz -e "ssh -p 2122" classifier_data_single \
lixiang@service.qich.top:/home/hddData/User/lixiang/hilserl_workspace/HIL-SERL-Project/examples/


14,目前本地运行后，actor判定一次reward=0.9以上后，会把这段数据发给learner，忽略报错，只是传输的慢而已，七分钟左右后learner开始x/1000000,
即learner接受actor的数据后开始训练
训练到成功保存checkpoint_y 后，例如checkpoint_14000，那么本地即actor记录的12000以前的buffer和demo_buffer可以删去。
保留1200-1400的buffer和demo_buffe即可，为了方便后续learner可以读取replay_buffer,
之前的删去是因为buffer和demo_buffe主要起回顾作用，回顾最新的即可

learner：
如果learner保存的checkpoints_y没问题，那么下次重新启动learner后，会先读取y，然后从0/（1000000-（y+1））继续训练。
如果读取checkpoints_y失败（权重文件有问题，不可用等），那么learner继续从0/1000000开始。
然后learener正式训练前等待读取的Filling up replay buffer是读取actor即本地保存的budder
所以本地保留一些buffer和demo_buffer更方便，buffer保证让learner更快度过Fillin，g up replay buffer，demo_buffer让learner拥有更多一些的数据

actor：
判定为给奖励后，会把数据发给learner，如果learner收到了，会开始复位，继续下一个任务执行
actor和learner类似，但不从ckpt读取（ckpt在服务器上，专门给learner读取），actor从buffer的最新transitions开始读取，所以buffer不要删除最新的几个
actor的buffer无所谓，训练完成一环节的基准是learner：z/z 训练完毕，actor只是提供数据而已

输出分析：reward=0.9以上后会复位？
感觉训练效果不好，是否路的数据有问题？


是的：成功给奖励后，这个成功 step 的 transition 会先进入 actor 本地队列；随后在 episode 结束分支里，actor 先发 stats、再 client.update() 把队列同步给 learner，最后才 env.reset() 开始复位。



问题：
不是“2 秒后的复位轨迹”本身导致夹爪瞬间闭合，而是你的夹爪控制语义和 HIL 官方默认的连续位置型 gripper 语义不匹配；尤其你描述的是“按一下切换一次”的夹爪，这和当前代码持续 publish JointState.position=[float(gripper)] 的模式本来就非常不像一套协议。
怀疑是actor发出的夹爪闭合指令，但是为什么读取我的demos会保持夹爪闭合，难道录制的夹爪数据和官方不匹配？
官方好像是一直闭合夹爪发出持续指令
我的手柄是一瞬间，但是，如果ros2 topic echo /hdas/feedback_gripper_right 的输出难道也是一瞬间吗？
我的可视化脚本显示夹爪数据平常保持在-0.9，夹爪闭合一瞬间对应-0.9然后立刻跳变回到+0.9
如果读取的是量程，即使手柄指令是一瞬间，那也不应该量程是一瞬间阿
  或者说量程没问题，是可视化界面没看明白怎么处理的数据（毕竟末端就是相对位置），或者处理数据方式和官方不适配

官方默认假设夹爪是“连续目标型/位置型/保持型”，而你的夹爪更像“按一下切换一次”的 toggle 型
但是直接读取量程就肯定没问题。

我的思路：
先排查：
       那就不读一瞬间的指令，直接读夹爪发出的量程：/hdas/feedback_gripper_right \ 
       开始训练，然后ros2 topic echo /hdas/feedback_gripper_right  看看输出

       你的排查方向是对的，但要改成“用 /hdas/feedback_gripper_right 做诊断，不要直接替代训练动作”。
因为你当前 demo / buffer 里保存的是 动作命令（actions / intervene_action），不是夹爪反馈；而真实反馈应该看 observation 里的 right_gripper 或直接看 /hdas/feedback_gripper_right。如果 feedback 是持续闭合而不是瞬时跳变，就说明问题主要在“你现在可视化看的其实是命令，不是量程”。


你这个录制脚本到底录了什么

在每一帧，它保存的是一个 transition，里面有：

observations
actions
next_observations
rewards
masks
dones
infos

而且这里的 actions 不是固定零动作；如果这一帧有人类接管，就会被替换成 info["intervene_action"]，
然后再存进去。也就是说，你现在 demo 里的 actions，本质上就是这一帧实际执行/接管的动作命令。

那hil需要的这个actions，我的和官方的夹爪不一样，所以必须修改对吧？


解决：
末端：可用，和官方一致
夹爪：修改， 自己定义量程范围规定张开还是闭合并改成稳定的值（-1/+1），反推到标签张开还是闭合（+1/-1）
    自己的机器人状态是瞬时的，只有一瞬间的信号，这样会让hil认为大多时间都在张开，一瞬间闭合，太难学习
修改：录制脚本+actor收集在线数据这两个脚本需要随之修改，从而让标签对应状态和官网一样




最新总结：
整体是通的。
结论分开说：

1）learner 正常在跑。
它已经成功绑定 5588，先做了 warmup 阶段的重复发布，然后 replay buffer 填满后进入正式训练；之后在 step=50/100/150/200/250/300/350/400... 都持续做了 train_periodic_update 发布新网络。

2）actor 也正常在跑。
actor 这边已经完成了 GPU、相机、ROS2、底层服务连接、模式切换、奖励分类器 ckpt 恢复，并且真正进入了 starting actor loop，已经至少跑完两个 episode，还有正常的复位、VR 接管、脚本接管。

3）learner 生成的网络，确实发给 actor 了；actor 也确实收到了。
证据非常直接：
learner 初始发布 checksum=8.368379，actor 第一次收到也是 8.368379，随后 actor 第一次应用的还是 8.368379。后面 learner 在 step=50 发布 8.407012，actor 的 recv_count=2 收到同一个值；step=100 发布 8.411232，actor 的 recv_count=3 收到同一个值；step=150/200/250/300/350/400 的 checksum 也都和 actor 的 recv_count=4/5/6/7/8/9 一一对上。

4）actor 不只是收到了，还应用上了。
日志里有明确的 actor-network-apply：
初始网络 applied_count=1，之后 applied_count=2/3/4/5 也都和 learner 后续发布的新 checksum 对得上。到第二个 episode 结束时，actor 打印的是 recv_count=10, applied_count=10，这说明到那个时刻为止，前 10 次收到的网络都已经应用了。后面 recv_count 继续涨到 17，只是由于你现在最小化日志，后续 apply 没有每次都打印出来。

5）actor 往 learner 回传数据这条链也是通的。
最强证据不是端口日志，而是 learner 端的数据变化：
warmup 后 learner 的 replay_buffer=1018，随后继续增长到 1688/1853/2038/2198/2348/...，说明 actor 的 transition 确实在送到 learner。更直接的是，actor 第一个 episode 结束时打印：return=0.0000, length=1500, duration=179.368, intervention_steps=1229；learner 随后也打印了同一条 episode stats：return=0.0000, length=1500, duration=179.368, intervention_steps=1229。这说明 actor 的 episode 统计也确实发到了 learner。

6）但通信不是完全完美，有过短暂超时。
actor 中间两次出现：
Failed to send message to localhost:5588: 资源暂时不可用, potential timeout。这表示 actor 到 learner 的 REQ/REP 通道有过瞬时卡顿。好在它没有把系统打断，因为后面 learner 还是继续训练、继续发布网络，actor 也继续收到 recv_count=2/3/4/.../17。所以这是有小毛刺，但整体可用。

7）奖励分类器 ckpt 也成功加载了。
actor 初始化时已经明确恢复了：
Restoring orbax checkpoint from .../classifier_ckpt_single/checkpoint_150，并在很短时间内完成加载。

8）唯一还不能说“已经学好了”的，是任务表现。
虽然系统链路已经通了，但当前训练效果还没起来：

actor 已经跑完两个 episode，都是 return=0.0000, success=0.0，而且 intervention 很多；learner 端收到的 episode stats 也是 0 回报。说明现在是系统正常运转，但策略还没学出成功行为。

下一步：
一，解决延迟：
首先本来就是异步运行：actor把数据发给learner，learner每固定步把网络发给actor更新actor的动作策略。
（1）actor通过REQ/REP传给learner的数据会卡顿，但好像只要传过去就可以，如果有影响就修改
（2）learner每五十（自己设置的）步传给actor最新网络，actor通过ssh接受应用的延迟在0.025s左右，最慢0.053，如果有影响就修改

二，需要解决：
（1）actor会卡顿一下的原因
（2）夹爪定义
（3）数据转换
（4）sac涉及视觉输入吗？还是只有奖励分类器使用了视觉？
（5）learner更新网络的速度（目前两张4090,速度3,4it/s）：
可以减少图像输入，降低图像分辨率


办法：
（1）奖励输入画面和策略输入画面不需要一样，画面是两条路线输入的，
所以可以奖励用一组画面(用于判定奖励)，推理用另一组画面（用于看画面输出动作）
所以我怀疑侧面一个相机即可实现奖励判定，
推理必须出现夹爪和场景关键点才可以合理输出动作
目前我的奖励和推理都公用三个画面，但是画面只有腕部相机可以看到夹爪，另外两个画面只能看到usb和插孔，看不到机器人夹爪和右臂